// 应用配置
export const appConfig = {
  appId: 'rect-words-app',
  name: 'Rect Words',
  version: '1.0.0'
};

// 主题配置
export const themes = {
  LIGHT: 'light',
  DARK: 'dark'
};

// 口音配置
export const accents = {
  US: 'us',
  UK: 'uk'
};

// 模块类型
export const modules = {
  LIBRARY: 'library',
  READER: 'reader',
  WORDBOOK: 'wordbook',
  REVIEW: 'review',
  SETTINGS: 'settings'
};

// 单词状态
export const wordStatuses = {
  NOT_STUDIED: 'not_studied',
  REVIEWING: 'reviewing',
  COMPLETED: 'completed',
  MASTERED: 'mastered'
};

// API配置
export const apiConfig = {
  dictionaryApi: '/api/dictionary/v2/entries/en'
};
